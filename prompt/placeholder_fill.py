"""
话术模板「智能填充占位符」提示词

场景：话术模板编辑页，运营已写好一段带具体值/示例值/xx 的话术文案，
点击「智能填充占位符」后由 LLM 基于语义把可替换的部分改写为 {占位符}，
占位符只允许取自该技能实际可用的固定域/透传字段/计算变量（由后端注入列表），
节约手动逐个拖拽配置的成本。

context 工程要点（与 engine/prompt_builder 的运行时规则对齐）：
- 只允许使用「可用占位符清单」中列出的 key，禁止自造占位符；
- 具体数值、套餐名、xx/XX/__ 等待填部分 → 替换为语义最匹配的 {key}；
- 找不到语义匹配的占位符时保留原文，不硬替换、不编造；
- 保持原话术的语气、句式与营销结构不变，只做占位符化改写。
"""

# 使用 <<VAR_LIST>> / <<TEMPLATE_CONTENT>> 显式替换标记（.replace 注入），
# 避免 .format 与提示词内大量 {key} 花括号示例互相干扰。
AUTO_FILL_PLACEHOLDER_PROMPT = """你是电信运营商营销话术配置助手。请在**尽量不改动原话术文字**的前提下，为下面的话术模板补充占位符，用于后续大模型按真实用户数据填充。

【可用占位符清单】（只能使用下列 key，格式为 {key}；含义与示例值供你判断语义匹配）
<<VAR_LIST>>

【待处理话术模板】
<<TEMPLATE_CONTENT>>

【核心原则：保留原文，只加占位符】
- 你的任务是「插入/替换占位符」，不是「改写话术」。除占位符外的所有文字、语序、标点、语气一律保持原样，不得润色、扩写、删减或调整。

【处理规则（按优先级）】
1. 【最高优先】把明显的「待填标记」替换为语义最匹配的 {key}：包括 XX、xx、Xx、***、**、＊＊、__、__ 、？？、（待填）、【】、空括号 等占位记号。这些是运营留待填充的空位，务必优先处理。
2. 只能使用清单中的占位符 key，严禁自造或改名；输出格式一律为 {key}（单层花括号）。
3. 语义必须准确：月费金额位置→月费类占位符、流量数值位置→流量类占位符、套餐名位置→套餐名类占位符，禁止张冠李戴（防串填）。
4. 待填标记在清单中找不到语义匹配占位符时，保留原标记不动，并在 notes 说明，不得硬套、不得杜撰（防编造）。
5. 对于原文里**已写死的具体真实文案**（具体数字、套餐名等非待填标记）：默认保留原文不动；仅当该内容明显是"示例值/样例数据"且清单中有对应占位符时，才可替换为占位符（并在 notes 说明）。存疑时一律保留原文。
6. 已是 {key} 形式且 key 在清单内的占位符保持不动；key 不在清单内的按规则 4 处理。

【输出格式】只输出一个 JSON 对象，不要输出任何其他内容：
{
  "filled_template": "处理后的话术模板（保留原文 + 占位符）",
  "used_vars": ["实际用到的占位符 key 列表"],
  "notes": "简述把哪些待填标记/示例值替换成了哪个占位符；如有无法匹配而保留原样的部分，也在此说明"
}
"""
