需要对子项目“自动配置智能体-老的“进行重构生成；

重构分为：
一、前端重构；
1、实现配置一键导入，不需要通过对话的方式进行配置；
   导入可以通过上传文件的方式进行导入；
   文件需要是标准格式，格式要求参考子项“话术智能体”的skill要求；

2、一键导入的生成的代码配置可以有前端预览；
3、一键导入可以分几个步骤操作，但是总体要提供操作提示，减少用户的疑问；
4、前端交互要简单，容易理解，操作要简单，不要过于复杂；

二、后端优化
1、根据导入的模板信息，自动化生成配置代码；
2、代码的生成方面，特别个性化的可以通过模型生成；
3、通用能力的代码可以复用”话术智能体“的代码；
4、生成代码的格式要求参考子项“话术智能体”的skill要求或者规范；
5、自动化生成的配置，可以通过发布无缝发布到“话术智能体”的技能中；并且可以在“话术智能体”的技能中进行查看和编辑；
6、智能体agent loop部分可以参考“自动化配置智能体-老的”的agent loop部分，但是需要根据导入的模板信息进行调整；
7、代码生成逻辑也可以参考“自动化配置智能体-老的”的代码生成逻辑，但是需要根据导入的模板信息进行调整；

整体要求：
1、一键导入功能，可以有多种方式进行导入，包括上传文件、粘贴文本等；
2、一键导入可以分几个步骤操作，但是总体要提供操作提示，减少用户的疑问；
3、自动配置智能体的定位是，辅助运营快速完成话术智能体skill的配置，减少算法人员的开发工作，一些特别个性化的代码可以通过模型生成；


重构后的代码放到子项目“自动配置智能体-新的”中；


页面需要优化：
一、配置页面内容：
1、配置区，包括：配置项、配置值、配置描述等；
2、预览区，包括：预览效果、预览代码等；
3、发布区，包括：发布按钮、发布进度、发布结果等；
4、帮助区，包括：帮助文档、帮助视频等；


二、配置，目前主要是导入，也可以通过选择菜单选择对应的模板进行配置；


自动配置智能体-新的项目，需要有验证和校验功能，验证是否跑通；
一键导入的配置，需要有验证和校验功能，验证是否跑通；
验证点需参考项目“话术智能体-生产“项目的验证点，例如验证接口，接口到话术模板槽位，等等；
如果没校验通过，需要给出提示，校验和提示可以通过大模型给出优化建议；
项目的前端交互可以整体优化下，引导式交互

前端需要用 VUE实现，参考“话术智能体-生产”项目的页面样式和交互，这样才能和灵运平台融合；

实现skill管理模块，可以对已经配置的 SKILL 进行查看，修改，删除，发布，回滚等操作；
其中查看可以查看skill的配置信息（接口、话术模板），包括：配置项、配置值、配置描述等；



skill 管理模块可以读取到skill-runtime的skill信息，包括：skill id、skill name、skill version、skill status、skill created time、skill updated time等；并且可以进行查看、修改、删除、发布、回滚等操作；



配置智能体页面需要优化：
1、省份和意图选择都为空，需要可以skill-runtime的skill信息进行选择；
2、一键导入页面优化：一键导入页面需要优化，减少用户的疑问，提供操作提示，减少用户的疑问；
   分成两种创建模式：
   1、复制已有skill的或者分场景的标准配置，进行修改；
   2、一键导入的置（已有的）；
3、两种模式，前端可以选择，默认是复制已有或者分场景的标准skill的配置，进行修改；


下面的页面需要优化
克隆已有 Skill 、一键导入配置，改成tab页面切换的方式
一键导入的配置页面操作需要优化，不需要内置模板库，都是通过配置文件上传；



自动配置智能体中的每个skill的接口和话术配置，不需要对json文件进行修改，直接通过配置页面进行修改，保存成json文件；
保存的json文件需要是标准格式，格式要求参考子项“话术智能体”的skill要求或者规范；
