# 营销话术推荐智能体能力介绍

> 项目路径：`/Users/liweizhong/Desktop/Code/营销话术智能体/znhs_refactor`

---

## 一、能力简介

### 1. 建设背景

在运营商呼叫中心及线上营销场景中，坐席人员每天需要向海量用户推荐套餐、营销活动及升档方案。传统话术生产高度依赖人工编写，存在以下痛点：

- **话术千篇一律**：难以结合用户真实用量、标签、当前套餐进行个性化表达；
- **数据分散难整合**：用户套餐、历史用量、推荐产品、用户标签等数据分散在不同接口，人工整合成本高；
- **省份差异大**：各省接口规范、产品字段、营销策略差异显著，传统硬编码方式导致系统僵化、扩展困难；
- **上线周期长**：新增一个营销场景往往需要排期开发、测试、发布，无法快速响应业务需求。

在此背景下，营销话术推荐智能体（znhs_refactor）采用 **"配置驱动 + 多智能体协同 + 大模型生成"** 的架构，将接口调用、数据标准化、推荐策略、话术生成等环节抽象为可配置的能力模块，实现营销话术的自动化、个性化、可扩展生产。

### 2. 核心价值

- **个性化营销话术生成**：基于用户画像与推荐产品，自动生成千人千面的营销话术；
- **配置化快速上线**：通过 SkillManager 运营后台即可完成省份、意图、接口、话术模板的配置与发布，无需修改代码；
- **多省份多场景复用**：统一标准数据域与插件化架构，支持快速复制到不同省份和营销场景；
- **运营自主可控**：技术支撑聚焦接口对接，运营人员可独立完成话术调优与上线。

### 3. 解决的问题

| 问题类型 | 具体表现 | 解决方式 |
|---------|---------|---------|
| 数据孤岛 | 用户套餐、用量、标签、推荐产品来自不同系统 | 通过 DataStep 并发调用多接口，统一映射到 7 大标准数据域 |
| 话术同质化 | 坐席背诵固定话术，难以打动用户 | 基于用户画像 + 套餐差异 + 大模型生成差异化话术 |
| 省份适配难 | 各省接口字段、产品形态不同 | 按省份+意图拆分 Skill 包，独立配置接口与模板 |
| 上线周期长 | 每新增场景都需要开发排期 | SkillManager 后台配置即发布，分钟级上线 |
| 效果难评估 | 无法快速验证话术与数据映射是否正确 | 提供测试弹窗、Prompt 实时预览、数据流映射闭环校验 |

---

## 二、能力功能及优势

### 1. 主要功能

#### 1.1 三步营销话术生成管道

系统核心执行链路为 **MarketingPipeline**，串行执行三步：

```
Step1 数据采集（DataStep）
  └── 并发调用外部接口，将响应数据映射到 7 大标准域

Step2 推荐筛选（RecommendStep）
  └── 按策略筛选 TopN 推荐产品（direct / 默认策略等）

Step3 话术生成（ScriptStep）
  └── 匹配话术模板 + 调用大模型生成最终营销话术
```

#### 1.2 7 大标准数据域（Context 工程）

将各省异构接口数据统一收敛到以下标准域，供话术模板消费：

| 标准域 | 含义 | 示例字段 |
|-------|------|---------|
| `current_package` | 用户当前套餐 | 套餐名称、价格、流量、语音 |
| `usage` | 历史用量 | 近 3/6 月平均流量、语音、饱和度 |
| `tags` | 用户标签 | 融合状态、是否超套、是否老旧套餐 |
| `user_info` | 用户基础信息 | 网龄、星级、终端类型 |
| `recommended_packages` | 推荐产品列表 | 推荐套餐名称、价格、流量、权益 |
| `user_profile` | 用户画像 | 流量偏好、语音偏好 |
| `domain_ext` | 扩展域 | 家庭业务、合约活动、订购信息 |

#### 1.3 双模式数据接入

| 模式 | 适用场景 | 说明 |
|------|---------|------|
| **接口查询模式** | 服务内部调用外部数据接口获取用户/产品信息 | 通过 `api_nodes` 配置接口请求与响应映射 |
| **直传模式** | 调用方（如 CTI/坐席系统）已持有用户/产品信息 | 通过 `extra_info` 直接传入，减少外部调用 |

#### 1.4 运营后台配置能力（SkillManager）

提供可视化运营后台，支持：

- **Skill 管理**：按省份 + 意图创建、编辑、发布、下线、删除 Skill；
- **接口配置**：配置接口请求、响应样例、出参映射（response_extract / field_transform）、智能映射；
- **标准数据关联**：查看接口产出覆盖的 7 大标准域；
- **话术模板配置**：配置模板内容、关联变量、关联接口、应用场景/环节；
- **数据流映射**：校验接口 → 标准域 → 话术模板的变量闭环；
- **测试验证**：在线输入手机号等参数，实时查看数据采集、推荐结果、生成话术；
- **版本与回滚**：支持配置版本管理与历史版本回滚。

#### 1.5 自动配置智能体（AutoConfigAgent）

支持上传 JSON 配置模板，一键生成并发布 Skill 包，降低首次配置门槛。

#### 1.6 多实例配置同步

- 支持 ES 持久化配置存储；
- 支持 Redis Pub/Sub + 定时轮询的混合同步模式；
- 配置发布后多实例自动刷新，保证线上服务一致性。

---

### 2. 应用场景

| 场景 | 说明 |
|------|------|
| **套餐推荐** | 根据用户当前套餐、用量及标签，推荐更优套餐并生成推荐话术 |
| **套餐升档** | 识别高价值/高用量用户，生成升档营销话术 |
| **营销活动推荐** | 针对特定营销活动，生成活动介绍及办理引导话术 |
| **家庭市场/个人市场** | 通过 `batch_contexts` 批量生成不同市场、不同环节、不同场景的话术 |
| **呼叫中心实时辅助** | 坐席通话中实时调用，获取个性化推荐与话术 |
| **线上客服/APP 推送** | 作为后台服务，为用户推荐产品并生成营销文案 |

---

### 3. 核心优势

#### 3.1 架构优势

- **多智能体协同**：用户信息融合、推荐策略、话术生成三个智能体职责清晰，可独立扩展；
- **无框架依赖的管道**：核心 `MarketingPipeline` 为纯 Python 异步实现，不依赖 LangGraph 等重型框架，部署轻量；
- **省份 + 意图级隔离**：Bundle 缓存按 `province:intent` 隔离，避免同省不同场景配置相互污染；
- **组件化扩展**：支持通过 `biz_config.pipeline` 定义自定义执行流，满足复杂场景。

#### 3.2 工程优势

- **配置驱动**：新增省份/场景无需改代码，通过 SkillManager 配置即可上线；
- **热重载**：配置发布后自动热加载，不中断服务；
- **多环境支持**：支持 development / gray / production / production_noauth 四种环境，灰度与生产可同进程运行；
- **高可用降级**：ES 异常时自动降级 Redis 或本地文件，保证服务可用性；
- **可观测性**：全流程 trace_id 追踪、阶段耗时记录、日志分级展示。

#### 3.3 业务优势

- **个性化程度高**：话术结合用户套餐、用量、标签、推荐产品差异，避免千篇一律；
- **响应速度快**：接口并发调用 + 实例级缓存 + LLM 连接池预热，满足实时外呼场景；
- **运营效率高**：从配置到上线分钟级，支持 A/B 测试与快速迭代；
- **风险可控**：版本管理、回滚机制、发布前测试校验，降低线上事故风险。

---

### 4. 典型流程示例

以 **北京-套餐推荐** 为例：

1. 用户来电，系统调用 `/znhs/marketing/recommend`；
2. DataStep 并发调用北京推荐接口，获取当前套餐、推荐套餐、用户标签；
3. 接口响应经 `response_extract` → `field_transform` 映射到 7 大标准域；
4. RecommendStep 按策略筛选 TopN 推荐套餐；
5. ScriptStep 匹配北京套餐推荐话术模板，注入 `current_package`、`usage`、`pkg_brief`、`diff_str` 等变量；
6. 大模型生成最终话术并返回给坐席系统。

---

*文档生成时间：2026-07-13*
