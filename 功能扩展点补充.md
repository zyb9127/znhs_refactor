# 功能扩展点补充

> 创建日期: 2026-08-06
> 最后更新: 2026-08-14

---

## 1. 模板关联变量 (linked_vars) 不会及时清理

- **位置**: `routers/management.py` → `_fill_placeholder_vars()`
- **问题**: linked_vars 只增不减。历史上模板引用过 `{table}` 时加入了 `"table"`，后来删除 `{table}` 重建模板，`"table"` 仍在 linked_vars 里，导致 `diff_table` 照样返回
- **根因**: `_fill_placeholder_vars` 从已有 linked_vars 出发只 extend，从不移除已不再引用的占位符变量
- **临时方案**: 直接改 `biz_config.json` 手动删除 linked_vars 中的冗余项
- **修复方向**: 占位符来源的变量应按当前模板内容重新计算，模板里不再引用的自动清理；API 域变量不受影响
- **备注**: 修复代码已提交（commit `3f4f02e`），但 shandong 配置中仍残留未清理的 `"table"`，需重启服务后重新保存模板触发清理

---

## 2. 话术模板批量导入：内容去重方案（方案A：过滤跳过）

- **位置**: `utils/skill_runtime.py` → `bulk_upsert_templates()`（唯一汇聚点，`POST /api/templates/bulk` 与废弃的 `/api/templates/import` 都走这里）
- **问题**: 批量导入话术模板当前只按 `template_id` 去重，而 CSV 导入行不带 template_id → 每条全新建。同一技能包内 `(产品ID, 环节, 场景)` 维度完全相同的模板（含同一 CSV 反复导入）会被重复导入成多条死模板。运行时匹配桶 key 就是这三个字段（`engine/template_index.py`），同一维度只取 priority/列表序靠前一条，其余永不生效
- **方案**: 导入时按 **`(product_id, stage, scene)`** 三元组（strip 归一化后完全一致）判重，重复项**跳过不写入**，并在响应里报告跳过数量
  - **判重键**：`product_id + stage + scene` 三个字段；**状态列（online/offline）不参与判重**
  - **话术内容不参与判重**：同维度不管内容是否一致都只留一条（与运行时"同维度只生效一条"语义对齐，从源头杜绝死模板）
  - **覆盖范围**：批内重复（CSV 内部两行同维度）+ 与库中存量同维度，一次判重（索引初始装载既有模板，每写一条即加入，先批内后存量）
  - **不影响**：单条手动新增/编辑（`upsert_template`，非批量路径）
- **改动点**:
  1. `bulk_upsert_templates`：增加 `_dim_key(t) = (product_id, stage, scene)` 维度索引，命中则 `continue`（跳过）；显式 template_id 合并优先于维度判重（编辑/更新不受影响）
  2. `routers/management.py` `bulk_create_templates`（L1166）：`skipped = len(data_list) - len(saved)`，message/data 报告跳过数
  3. `frontend/.../SkillConfigEditor.vue` `doImportCsv`（L3641）：成功提示追加「跳过 N 条重复」
  4. `tests/test_skill_publisher.py`：新增维度判重用例（重复跳过 / 批内去重 / 同内容不同维度不误伤）
- **运营影响**: 同一维度模板想"换话术内容"不能靠重新导入覆盖（会被判重拦掉），需先删旧模板再导，或直接编辑该条。实际使用模式为"导入前先删旧模板"，故过滤仅作兜底，风险低
- **不采用方案B（覆盖更新）**: 覆盖需按维度查出旧 template_id 并复用 tid 合并逻辑，且依赖判重键填写的稳定性，可能误伤数据；当前"先删再导"工作流下过滤方案已满足需求
- **备注**: 判重键稳定性取决于 CSV 中 `产品ID/环节/场景` 列填写是否一致（自由文本、写法不同会被当不同维度）；模板管理页（TemplateConfig）已废弃不用，实际导入入口只有 SkillManager → 技能包编辑器 → 导入 CSV

---

## 3. 「导入并保存」按钮优化：成功后复位，防止重复点击重复导入

- **位置**: `frontend/.../SkillConfigEditor.vue` → 导入 CSV 弹窗（L1435 弹窗、L1513-1518 按钮、L3641 `doImportCsv`）
- **问题**: 「导入并保存」按钮可无限次点击，每点一次就执行一次导入。浏览器 `File` 对象是**选中时的快照**，磁盘上编辑原文件不会反映到已选文件里；不重新选文件就反复点，导入的永远是同一份旧内容（配合第 2 条去重，重复导入即使被后端拦掉也属于多余操作，且产生"跳过 N 条"的噪音反馈）
- **方案**: 导入成功后**清空已选文件**，弹窗回到初始"拖拽/点击选择文件"状态，按钮因 `:disabled="!csvFile"` 自动置灰，必须重新选文件才能再次导入
- **改动点**:
  1. `doImportCsv` Mode A（`props.province && props.intent`，导入即保存）分支：导入**成功**后 `csvFile.value = null`；**失败不置灰**，允许原地重试（网络/后端错误时不丢已选文件）
  2. Mode B（新建配置、追加本地列表）分支：同样在成功追加后清空，逻辑一致
  3. `clearCsvFile`（现有「移除」按钮）已具备重置能力，重新选文件走 `onCsvUploadChange` 恢复可点，无需新增状态
- **效果**: 导入成功 → 文件卡片消失、回到上传区、"导入并保存"变灰 → 要再导必须重新拖/点选文件（拿新快照），从源头杜绝"同一会话内反复点同一文件"
- **与第 2 条的关系**: 前端复位防手滑（体验层）+ 后端维度去重兜数据一致性（数据层），互为双保险；换文件名重导、隔天重导等同批次数据仍由后端去重拦截
- **备注**: 弹窗 `destroy-on-close` 只销毁弹窗 DOM，`csvFile` 是组件级 ref，不随弹窗关闭重置——方案无需改这一点，成功即清空已覆盖"重开弹窗仍可再导"的场景
