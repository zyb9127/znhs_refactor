# 话术生成逻辑完整报告

## 目录

1. [总体流程](#1-总体流程)
2. [入参说明](#2-入参说明)
3. [配置加载](#3-配置加载)
4. [模板匹配：三阶段12档降级](#4-模板匹配三阶段12档降级)
5. [Prompt 组装：四重过滤机制](#5-prompt-组装四重过滤机制)
6. [LLM 生成与降级兜底](#6-llm-生成与降级兜底)
7. [后处理与返回格式](#7-后处理与返回格式)
8. [完整示例：全量参数实战](#8-完整示例全量参数实战)
9. [文件索引](#9-文件索引)

---

## 1. 总体流程

```
POST /marketing/recommend
        │
        ▼
┌──────────────────────────────────────────┐
│  Pipeline 三步管道 (core/pipeline.py)      │
│  Step1: DataStep   → 并发调外部接口/直传    │
│  Step2: RecommendStep → 策略筛选 TopN      │
│  Step3: ScriptStep → 模板匹配 + LLM 生成    │
└──────────────────────────────────────────┘
        │
        ▼
   JSONResponse {
     code: 200,
     data: { callId, phone, intent, province, recommend_results }
   }
```

当请求中包含 `batch_contexts` 时，ScriptStep 走 `run_batch()` 批量模式；否则走 `run()` 单维度模式。本报告以批量模式为主。

### 关键文件

| 文件 | 职责 |
|------|------|
| `routers/realtime.py` | 接口入口，Pydantic 校验，组装返回 |
| `core/context.py` | `FlowContext` 上下文对象 |
| `core/pipeline.py` | `MarketingPipeline` 三步管道编排 |
| `steps/script_step.py` | Step3 话术生成主逻辑 |
| `engine/prompt_builder.py` | Prompt 组装与四重过滤 |
| `engine/template_selector.py` | 模板匹配三阶段12档 |
| `prompt/script_generation.py` | 生成规则常量 |
| `plugins/package_diff.py` | 套餐差异计算 |
| `skills-runtime/{province}/{intent}/config/biz_config.json` | 模板配置 |
| `skills-runtime/{province}/{intent}/config/api_nodes.json` | 接口/直传配置 |

---

## 2. 入参说明

### 主参数

```json
{
  "intent":       "套餐升档",   // 业务意图，与技能包目录名一致
  "province":     "shandong",  // 省份代码
  "phone":        "150...",    // 手机号
  "topN":         3,           // 推荐产品数（1-10）
  "callId":       "可选追踪ID"
}
```

### extra_info（直传数据）

```json
{
  "extra_info": {
    "uniProdGrade":  6,     // 当前套餐档位（元）
    "voiceTotal":    30,    // 当前通话分钟数
    "totalFlow":     50,    // 当前流量（GB）
    "flow":          100,   // 新套餐流量
    "giftFlow":      70,    // 赠送流量
    "shareFlow":     150,   // 共享流量
    "tf":            37,    // 总流量
    "internetAge":   15,    // 网龄（年）
    "telNumStar":    8,     // 星级
    "discountPrice": 11,    // 折扣价
    "origPrice":     88,    // 原价
    "reducePrice":   77     // 优惠后价格
  }
}
```

`extra_info` 的 key 必须与模板占位符名一致（大小写敏感），值可以是数字或字符串。

### batch_contexts（批量话术生成控制）

```json
{
  "batch_contexts": [
    {
      "product_id": "20221117085127",  // 目标产品ID
      "stage":      "营销推荐",         // 营销环节
      "scence":     "营销推荐",         // 场景（注意接口字段拼写）
      "expand":     false              // 是否枚举所有scene
    }
  ]
}
```

| 字段 | 必填 | 说明 |
|------|------|------|
| `product_id` | 否 | 空则对推荐结果所有产品展开；非空则只处理指定产品 |
| `stage` | 否 | 营销环节维度，匹配模板用 |
| `scence` | 否 | 场景维度，匹配模板用 |
| `expand` | 否 | true + scence为空 → 枚举 product_id+stage 下所有scene各生成一条 |

---

## 3. 配置加载

`_load_biz()` 从 `biz_config.json` 解析运行时配置：

| 配置段 | 字段 | 默认值 | 说明 |
|--------|------|--------|------|
| `strategy` | `max_script_length` | 150 | 话术最大字数 |
| `strategy` | `max_parallel_scripts` | 3 | LLM 并发度 |
| `strategy` | `llm_max_concurrency` | 0(不限) | LLM 请求上限 |
| `field_aliases` | `pkg_name/pkg_fee/pkg_flow/pkg_voice/product_id` | 内置默认 | 产品字段别名映射 |
| `script_templates_v2` | 模板列表 | [] | 话术模板（核心配置） |
| `prompts` | `package_recommendation` | {} | 旧格式兜底 |

---

## 4. 模板匹配：三阶段12档降级

**文件**: `engine/template_selector.py` → `select_template_linear()`

**基础维度**: `intent`（技能包已按 `province` + `intent` 加载模板，此处只需过滤同 intent）

**候选维度**: `product_id`（产品）、`stage`（环节）、`scence`（场景），每个维度均可为空表示"不限"

```
阶段1：产品精确匹配（product_id 完全一致）
  ├─ 1. 精确 + 环节 + 场景     ← 最佳命中
  ├─ 2. 精确 + 环节
  ├─ 3. 精确 + 场景
  └─ 4. 仅精确

阶段2：产品模糊匹配（关键词子串，仅 product_id 非空时触发）
  ├─ 5. 模糊 + 环节 + 场景
  ├─ 6. 模糊 + 环节
  ├─ 7. 模糊 + 场景
  └─ 8. 仅模糊

阶段3：通用兜底（product_id 为空的模板，不限产品）
  ├─ 9.  通用 + 环节 + 场景
  ├─ 10. 通用 + 环节
  ├─ 11. 通用 + 场景
  └─ 12. 兜底全空（意图级通用）
```

**触发规则**:
- `product_id` 非空 → 阶段 1 → 阶段 2 → 阶段 3 依次尝试
- `product_id` 为空 → 直接跳阶段 3

**模糊匹配**: 模板 `product_id` 按逗号/换行拆分关键词 → 与请求 product_id 互包含即命中。适用于"一条模板对应多个产品"的场景。

**索引加速**: 模板数 ≥ 30 且环境变量 `ZNHS_TEMPLATE_INDEX` 不为 `"0"` 时启用哈希索引（`engine/template_index.py`），支持 `priority` 字段排序。

### 匹配成功后提取的模板信息

| 模板字段 | 用途 |
|----------|------|
| `template_content` | 话术模板正文，含 `{占位符}` 或 `[占位符]` |
| `linked_vars` | 期望关联的标准域变量列表（空数组则走自动注入） |
| `script_requirement` | 追加到生成规则末尾的第5条话术要求 |
| `prompt_template` | 自定义 Prompt 模板（为空则用 `template_content`） |
| `status` | 仅匹配 `online` 状态的模板 |

---

## 5. Prompt 组装：四重过滤机制

**文件**: `engine/prompt_builder.py` → `build_prompt()`

### 5.1 Prompt 五段式结构

```
┌───────────────────────────────────────────────┐
│ 段落1: 角色/任务说明                           │
│   你是套餐营销推荐坐席，负责将【上下文数据】填充   │
│   进【话术模板】，生成自然、口语化的个性化话术...  │
├───────────────────────────────────────────────┤
│ 段落2: 【上下文数据】                           │
│   经接口映射得到的真实用户与套餐数据，              │
│   是你唯一可依据的事实来源，                      │
│   请勿使用未在此列出的信息                        │
│                                               │
│   变量标签 {占位符名}：值                        │
│   变量标签 {占位符名}：值                        │
│   ...                                         │
├───────────────────────────────────────────────┤
│ 段落3: 【话术模板】                             │
│   模板正文（含 {槽位} [槽位]，已被过滤层清理）     │
├───────────────────────────────────────────────┤
│ 段落4: 【生成规则】                             │
│   1. 不得编造不存在的数字/套餐名/优惠/功能/权益    │
│   2. 缺失/为空/为0则跳过，不得借用其他字段值       │
│   3. 占位符按上下文锚点对应填充，缺失的跳过         │
│   4. 模板是语言基准，只需替换占位符                │
│   5. 话术要求（来自模板的 script_requirement）    │
├───────────────────────────────────────────────┤
│ 段落5: 输出指令                                 │
│   请直接输出话术文本，不需要任何前缀标签：         │
│   话术：                                       │
└───────────────────────────────────────────────┘
```

### 5.2 上下文数据注入：四重过滤

这是本轮重构的核心改动。目标：**LLM 只能看到模板真正需要、且确实有值可填的数据**。

```
                   extra_info（调用方传入的全部字段）
                         │
┌────────────────────────┼────────────────────────┐
│ 过滤层1 — passthrough 通道                          │
│   只注入模板占位符中实际引用的字段                       │
│   if pk not in tpl_token_set → skip                │
│   例: flowStandard=5 不在模板占位符中 → 不出现在 Prompt │
└────────────────────────┼────────────────────────┘
                         │
                         ▼
                   上下文数据行
                   uniProdGrade {uniProdGrade}：6
                   internetAge {internetAge}：15
                   flow {flow}：100
                   ...
                         │
┌────────────────────────┼────────────────────────┐
│ 过滤层2 — 模板清理                                    │
│   未解析的占位符 → 按逗号删除所在分句（非整句）            │
│   token not in emitted → 删分句                      │
│   例: [telNumStar] 缺失 → 删除 "您是咱移动15年的8星级"   │
│       而非删除整句（保护同句其他有值占位符）               │
└────────────────────────┼────────────────────────┘
                         │
                         ▼
                   模板残余
                   [uniProdGrade]元的校园套餐
                   [voiceTotal]分钟和[totalFlow]流量
                   （[internetAge]/[telNumStar] 所在分句被删）
                         │
┌────────────────────────┼────────────────────────┐
│ 过滤层3 — 回扫清理（僵尸数据）                          │
│   上下文中有但模板中已不存在的变量 → 从上下文移除           │
│   对上下文逐行检查：该行标注的占位符还在模板残余中吗？        │
│   不在 → 移除该行                                     │
│   例: internetAge 所在句被删 → 上下文也清掉 internetAge   │
└────────────────────────┼────────────────────────┘
                         │
                         ▼
                   最终上下文
                   只剩模板残余中实际存在的占位符
                         │
┌────────────────────────┼────────────────────────┐
│ 过滤层4 — 模板全空 → 降级                             │
│   若清理后模板为空 → build_prompt 返回 ""             │
│   → _skip_llm=True → 跳过 LLM → 走降级话术           │
└────────────────────────┴────────────────────────┘
```

### 5.3 上下文注入的三条通道

| 通道 | 触发条件 | 说明 |
|------|----------|------|
| **linked_vars 通道** | 模板配置了 `linked_vars` | 使用标准域变量标签(`VAR_LABELS`)，支持同义组去重 |
| **passthrough 直传通道** | `api_nodes.json` 配置了 `passthrough_fields` | 直接注入 `extra_info` 中被配置选中的字段，**须经过滤层1** |
| **自动补齐通道** | 模板引用了占位符但前两个通道未注入 | 从 `extra_info` 顶层按变量名查找并注入 |

### 5.4 复合占位符计算

模板中可以使用数学表达式占位符（如 `[shareFlow-flow]`、`[flow+giftFlow-tf]`），`_try_compute_compound()` 负责计算：

- 先精确匹配 `extra_info` 中的 key
- 再按 `+`/`-` 拆分，逐个从 `extra_info` 取变量值
- 全部找到则从左到右计算，任一缺失则不注入
- 无法计算时走过滤层2按分句删除

---

## 6. LLM 生成与降级兜底

```
_prepare_one_script_sync()
      │
      ├─ 模板匹配 (select_template)
      ├─ PackageDiff 计算套餐差异
      ├─ build_prompt() 组装 Prompt
      │     │
      │     ├─ prompt 非空
      │     │     │
      │     │     ├─ LLM 成功 → _post_process() → _finalize_script_one()
      │     │     └─ LLM 失败 → _fallback_text()
      │     │
      │     └─ prompt 为空 (_skip_llm=True)
      │           → 不调 LLM → 直接 _fallback_text()
      │
      └─ 返回 { product_id, rank, marketing_text, ... }
```

### 降级话术

```python
def _fallback_text(self, pkg, diff):
    # 字段名通过 field_aliases 解析
    name = get_field(pkg, ["offerName", "package_name", "productName", "name"]) or "推荐套餐"
    fee  = get_field(pkg, ["initFee", "monthly_fee", "price", "fee"])
    flow = get_field(pkg, ["offerFlow", "data_quota", "dataGB", "flow"])
    parts = [f"推荐您升级{name}"]
    if fee:  parts.append(f"仅需{fee}元/月")
    if flow: parts.append(f"流量{flow}GB")
    return "，".join(parts) + "，回复1立即办理。"
```

### 后处理

```python
def _post_process(text):
    # 1. 多段落合并为一行
    # 2. 去首尾引号
    # 3. 无中文则置空（→ 触发降级）
```

---

## 7. 后处理与返回格式

### 接口返回

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "callId": "1505417137515054171375123",
    "phone": "15054171375",
    "intent": "套餐升档",
    "province": "shandong",
    "recommend_results": [
      {
        "product_id":     "20221117085127",
        "rank":           1,
        "marketing_text": "您现在的校园套餐只有30分钟和50G流量...",
        "diff_table":     null,
        "stage":          "营销推荐",
        "scence":         "营销推荐"
      }
    ]
  },
  "other_info": null
}
```

### recommend_results 字段说明

| 字段 | 说明 | 出现条件 |
|------|------|----------|
| `product_id` | 推荐产品ID | 始终 |
| `rank` | 排名 | 始终 |
| `marketing_text` | 生成的话术文本 | 始终 |
| `diff_table` | 套餐差异表格 | 仅 `linked_vars` 含 `"table"` 或 `prompt_template` 含 `{table}` |
| `stage` | 营销环节 | 仅批量模式 |
| `scence` | 场景 | 仅批量模式 |

---

## 8. 完整示例：全量参数实战

### 8.1 入参

```json
{
  "callId": "1505417137515054171375123",
  "intent": "套餐升档",
  "phone": "15054171375",
  "topN": 1,
  "province": "shandong",
  "extra_info": {
    "uniProdGrade": 6,
    "voiceTotal": 30,
    "totalFlow": 50,
    "flow": 100,
    "shareFlow": 150,
    "giftFlow": 70,
    "tf": 37,
    "internetAge": 15,
    "telNumStar": 8,
    "discountPrice": 11,
    "origPrice": 88,
    "reducePrice": 77
  },
  "batch_contexts": [
    {
      "product_id": "20221117085127",
      "stage": "营销推荐",
      "scence": "营销推荐"
    }
  ]
}
```

### 8.2 命中的模板

`template_id`: `tpl_shandong_1784625724532_17034c`

**`template_content`**（17个占位符，`[]` 格式）:

> 关注到您现在用的是[uniProdGrade]元的校园套餐，仅包含[voiceTotal]分钟和[totalFlow]流量，像寒暑假期间或者出去旅游，这样校园外的通用流量比较少，为方便您校外使用，现在给您优化一下，不仅套餐流量可以扩容到[flow]GB流量，每月额外再扩容[shareFlow-flow]GB可共享流量和[flow+giftFlow-shareFlow]GB非共享流量，总共就有[flow+giftFlow]GB流量了，比您现在用的多[flow+giftFlow-tf]GB。而且您是咱移动[internetAge]年的[telNumStar]星级贵宾客户，可以给您申请[discountPrice]元的套餐折扣，套餐原价每月[origPrice]元，两年内每月您只需交[reducePrice]元，到期恢复原价前都有短信提醒。另外还有百家会员权益。现在给您办理下个账期就可以用了。

**`script_requirement`**:

> 结合【上下文数据】中的当前套餐、历史用量与用户标签，先点出最突出的用户痛点，再用推荐套餐对应字段的真实值说明如何解决；只讲有数据支撑的卖点，口语化、可直接对客播报，150字以内，结尾自然引导办理。

**`linked_vars`**: `[]`（空，走自动注入）

### 8.3 占位符解析

模板扫描提取 17 个占位符：

| 类型 | 占位符 | extra_info 中是否有值 | 处理 |
|------|--------|----------------------|------|
| 简单 | `uniProdGrade` | ✅ 6 | 上下文注入 + emitted |
| 简单 | `voiceTotal` | ✅ 30 | 上下文注入 + emitted |
| 简单 | `totalFlow` | ✅ 50 | 上下文注入 + emitted |
| 简单 | `flow` | ✅ 100 | 上下文注入 + emitted |
| 复合 | `shareFlow-flow` | ✅ 150-100=50 | 计算后注入 |
| 复合 | `flow+giftFlow-shareFlow` | ✅ 100+70-150=20 | 计算后注入 |
| 复合 | `flow+giftFlow` | ✅ 100+70=170 | 计算后注入 |
| 复合 | `flow+giftFlow-tf` | ✅ 100+70-37=133 | 计算后注入 |
| 简单 | `internetAge` | ✅ 15 | 上下文注入 + emitted |
| 简单 | `telNumStar` | ✅ 8 | 上下文注入 + emitted |
| 简单 | `discountPrice` | ✅ 11 | 上下文注入 + emitted |
| 简单 | `origPrice` | ✅ 88 | 上下文注入 + emitted |
| 简单 | `reducePrice` | ✅ 77 | 上下文注入 + emitted |

**全部 17 个占位符有值可填** → 过滤层2不触发任何分句删除 → 过滤层3不触发 → 模板完整保留。

### 8.4 最终发给 LLM 的完整 Prompt

```
你是套餐营销推荐坐席，负责将【上下文数据】填充进【话术模板】，生成自然、口语化的个性化套餐营销推荐话术。
【上下文数据】（经接口映射得到的真实用户与套餐数据，是你唯一可依据的事实来源，请勿使用未在此列出的信息）
uniProdGrade {uniProdGrade}：6
voiceTotal {voiceTotal}：30
totalFlow {totalFlow}：50
flow {flow}：100
internetAge {internetAge}：15
telNumStar {telNumStar}：8
discountPrice {discountPrice}：11
origPrice {origPrice}：88
reducePrice {reducePrice}：77
shareFlow-flow {shareFlow-flow}：50
flow+giftFlow-shareFlow {flow+giftFlow-shareFlow}：20
flow+giftFlow {flow+giftFlow}：170
flow+giftFlow-tf {flow+giftFlow-tf}：133
【话术模板】
关注到您现在用的是[uniProdGrade]元的校园套餐，仅包含[voiceTotal]分钟和[totalFlow]流量，像寒暑假期间或者出去旅游，这样校园外的通用流量比较少，为方便您校外使用，现在给您优化一下，不仅套餐流量可以扩容到[flow]GB流量，每月额外再扩容[shareFlow-flow]GB可共享流量和[flow+giftFlow-shareFlow]GB非共享流量，总共就有[flow+giftFlow]GB流量了，比您现在用的多[flow+giftFlow-tf]GB。而且您是咱移动[internetAge]年的[telNumStar]星级贵宾客户，可以给您申请[discountPrice]元的套餐折扣，套餐原价每月[origPrice]元，两年内每月您只需交[reducePrice]元，到期恢复原价前都有短信提醒。另外还有百家会员权益。现在给您办理下个账期就可以用了。
【生成规则】
1. 仅依据【上下文数据】中的事实填充话术模板，不得编造数据中不存在的数字、套餐名、优惠、功能或权益。
2. 若某项信息缺失、为空或为 0，则跳过对应表述，既不提及、也不得用其他字段的值代替（例如语音为 0 则不谈语音；优惠月数为 0 则表述为"连续包月"而非"连续 0 个月"）。
3. 占位符对应关系：【上下文数据】每行已用 {占位符} 或 [占位符] 标注其对应的槽位，请将【话术模板】中出现的同名 {占位符} 或 [占位符] 替换为该行的事实值（按语义就近对应，不要张冠李戴）；模板中出现但【上下文数据】未列出的占位符按第 2 条处理（跳过、不臆造、不串填）。含运算的复合占位符（如 {a-b}、{a+b}）若任一参与计算的值缺失或未在上下文中出现，整句跳过，严禁从其他字段借用数值凑数计算。
4. 话术模板是你的语言基准：已有具体措辞的句子不得改写为同义表达，只需替换其中的占位符；只有模板明确为空或仅含方向性提示（如"介绍套餐卖点"）时，才可依据上下文数据自行组织话术。若第5条话术要求（如字数限制、风格侧重）与模板篇幅或内容冲突，以第5条为准，可对模板做必要的精简或调整以满足第5条要求。最终结果不得残留任何 {} 或 [] 占位符。
5. 话术要求：结合【上下文数据】中的当前套餐、历史用量与用户标签，先点出最突出的用户痛点，再用推荐套餐对应字段的真实值说明如何解决；只讲有数据支撑的卖点，口语化、可直接对客播报，150字以内，结尾自然引导办理。
请直接输出话术文本，不需要任何前缀标签：
话术：
```

### 8.5 LLM 实际返回

> 您现在的校园套餐只有30分钟和50G流量，校外用起来不太够吧？现在给您优化一下，流量直接扩容到100G，每月还多50G共享和20G非共享流量，总共170G，比现在多133G。您是移动15年的8星贵宾，能申请11元折扣，原价88元，两年内每月只需交77元，到期前有提醒。下个账期就能用，现在给您办上吧？

### 8.6 占位符填充对照

| 模板原文 | 填充值 |
|----------|--------|
| `[uniProdGrade]`元的校园套餐 | **6**元的校园套餐 |
| `[voiceTotal]`分钟 | **30**分钟 |
| `[totalFlow]`流量 | **50G**流量 |
| 扩容到`[flow]`GB流量 | 扩容到**100G**流量 |
| 多`[shareFlow-flow]`GB可共享 | 多**50G**共享 |
| `[flow+giftFlow-shareFlow]`GB非共享 | **20G**非共享 |
| 总共`[flow+giftFlow]`GB | 总共**170G** |
| 多`[flow+giftFlow-tf]`GB | 多**133G** |
| `[internetAge]`年 | **15**年 |
| `[telNumStar]`星级 | **8星** |
| `[discountPrice]`元折扣 | **11元**折扣 |
| 原价`[origPrice]`元 | 原价**88**元 |
| 交`[reducePrice]`元 | 交**77**元 |

**模板 17 个占位符全部正确填充，无编造、无串填、无残留。150字以内。**

---

## 9. 文件索引

| 文件 | 说明 |
|------|------|
| `routers/realtime.py` | 接口入口 `POST /marketing/recommend`，Pydantic 入参校验，组装返回 JSON |
| `core/context.py` | `FlowContext` 上下文对象，`to_result()` 构建 `recommend_results` |
| `core/pipeline.py` | `MarketingPipeline` 三步管道编排 |
| `steps/script_step.py` | `ScriptStep` 话术生成：`run()`（单维度）、`run_batch()`（批量）、`_prepare_one_script_sync()`（模板匹配+Prompt组装）、`_finalize_script_one()`（结果组装）、`_fallback_text()`（降级） |
| `engine/prompt_builder.py` | `build_prompt()`：五段式 Prompt 组装 + 四重过滤 + 复合占位符计算 + 分句删除 + 回扫清理 |
| `engine/template_selector.py` | `select_template_linear()`：三阶段12档模板匹配；`select_template()`：统一入口（索引/线性回退） |
| `engine/template_index.py` | 模板哈希索引（≥30条时自动启用） |
| `prompt/script_generation.py` | `SCRIPT_SYSTEM_HEADER`、`SCRIPT_GEN_RULES` 等 Prompt 常量 |
| `plugins/package_diff.py` | `PackageDiff`：当前套餐与推荐产品差异计算 |
| `services/llm_service.py` | `llm_service.generate()`：LLM 调用 |
| `schemas/template.schema.json` | 话术模板字段 Schema 定义 |
| `schemas/standard_domains.json` | 7大标准域 + 13项变量标签 + 11项 linked_vars 候选 |
