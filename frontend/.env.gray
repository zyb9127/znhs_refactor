# 灰度环境配置
# 构建命令：npm run build:gray
#
# 静态资源与入口路径前缀为 /znhs-gray/（与网关一致；后端默认 GRAY_SPA_PREFIX=/znhs-gray）

# 子应用环境标识（true = 作为 wujie 子应用运行）
VITE_SUB_APP_ENVIRONMENT=true

# 灰度环境标识，Vite base 为 /znhs-gray/
VITE_APP_PREFIX=gray

# 开发服务器端口
VITE_APP_PORT=20070

VITE_API_URL_PREFIX=/gray
