# 操作要点补充

> 创建日期: 2026-08-05
> 最后更新: 2026-08-10

---

## 1. 单产品与多产品统一入参结构

- 单产品和多产品使用同一个入参结构：`extra_info.recommended_packages` 数组
- 单产品传 1 个元素，多产品传多个元素
- 旧版 `extra_info.final_recommendations` 对象写法仍兼容（广东旧版），新对接不推荐使用
- `topN` 在直传模式下不再截断：传多少个产品就返回多少条话术

---

## 2. 比价入参有单独的参数规范，注意参数命名

- 比价由 `plugins/package_diff.py` 的 `PackageDiff` 类计算，按字段名**精确等值匹配**白名单，不在白名单内的字段名直接跳过
- **价格**: `initFee` / `monthly_fee` / `fee` / `price` / `curOfferFee`
- **流量**: `offerFlow` / `data_quota` / `flow` / `data` / `flowGb`
- **语音**: `offerVoice` / `voice_quota` / `voice` / `minutes`
- **宽带**: `bandWidth` / `bandwidth` / `broadband_speed`
- **副卡**: `suppCard` / `supp_card` / `subCard`
- **权益**: `otherRight`（仅推荐产品侧）
- 常见坑：`actual_price`、`baseFlow`、`voice_minutes`、`recommend_actual_price`、`recommend_total_flow` 等**不在白名单内**，会算出流量"—"、语音"—"；价格能碰巧从套餐名"38元档"兜底抠出来，但不可靠
- `biz_config.json` 里的 `field_aliases` 是另一套体系（出参 `monthly_fee`/`data_quota` 走它），PackageDiff 不读

---

## 3. diff 比价信息不支持外部传入，只认系统自动计算

- 接口文档（`docs/营销话术推荐服务接口规范（对外标准版）v1.0.md`）7.3 写了"调用方自算差异"传 `diff` 子对象的用法，但**代码目前不认**
- `PackageDiff` 只用自己的白名单现场算比价，产品条里的 `diff` 对象没有人读
- 透传子字段层也会把 `diff` 过滤掉（`isinstance(_lv, dict)` 跳过嵌套对象）
- 文档与代码对不上，要么文档写早了（设计意图未落地），要么后续改了。待确认后修复

### 补充：子路径占位符写法可以变通取到 diff 子字段

虽然模板编辑器的变量下拉列表选不到 `diff` 及其子字段（`recommended_packages[].diff` 是嵌套 dict，前端调色板生成逻辑里被 `isinstance(_lv, dict)` 过滤掉了），但**后端解析链路本身支持多级子路径**。

在模板文本中直接手写子路径占位符即可取到值：

```
{recommended_package[diff][diff_actual_price]}
{recommended_package[diff][base_flow_diff]}
{recommended_package[diff][voice_minutes_diff]}
```

解析路径：`prompt_builder.py` → `_subfield_roots["recommended_package"]` 指向当前产品 dict → `[diff][diff_actual_price]` 逐层取值。写裸字段名 `{diff_actual_price}` 不行（解析链路上不会穿透到 `pkg.diff` 里找）。

注：`{recommended_package[xxx]}` 这个根名也接受 `pkg_brief`、`pkg_name` 别名（都是同一条产品 dict）。

---

## 4. 产品 ID 取值优先级

- 产品 ID 优先从 `recommended_packages[].offerId` 获取
- 若 `offerId` 为空或不存在，其次从 `batch_contexts[].product_id` 取值
- 产品关联用 `offerId`（响应 `recommend_results[].offerId` 和 `product_id` 均回显此值），模板匹配也用 `offerId`（命中产品专属模板），`batch_contexts.product_id` 仅作兜底匹配提示

---

## 5. 话术模板匹配规则

### 5.1 一句话总结

模板匹配只认三个维度：**产品（product_id）→ 环节（stage）→ 场景（scene）**，按精确→模糊→通用逐级降级，产品没配上会自动找 `product_id=""` 的通用模板。

### 5.2 产品来源

系统从 `extra_info.recommended_packages`（直传模式）或外部接口（接口查询模式）拿到推荐产品列表后，对每个产品分别匹配模板、分别生成话术。

传给模板匹配的 "产品标识" 不是只有一个值，而是按顺序尝试：

```
① offerId（产品自身 ID）
② offerName / package_name / productName（产品名称）
③ recommend_package_name
④ batch_contexts.product_id（入参里的产品提示，兜底）
```

第一个命中模板的即胜出，后面的不再尝试。所以即使 `offerId` 没有专属模板，`offerName` 里的关键词也可能命中（如产品名包含 "升" 命中 product_id="升" 的模板）。

### 5.3 匹配优先级（从高到低）

所有模板先过滤：同 intent（或模板 intent 为空）+ status=online（无 online 时降级用 offline）+ 非 deleted。

然后按以下顺序匹配，命中即停：

| 优先级 | 匹配方式 | 说明 |
|---|---|---|
| 1 | 产品精确 + stage + scene | product_id 完全一致 |
| 2 | 产品精确 + stage | scene 不限 |
| 3 | 产品精确 + scene | stage 不限 |
| 4 | 仅产品精确 | stage、scene 都不限 |
| 5 | 产品模糊 + stage + scene | 产品 ID 关键词包含匹配（如模板配 "套餐,升档"，请求 "5G畅享套餐" 命中） |
| 6 | 产品模糊 + stage | |
| 7 | 产品模糊 + scene | |
| 8 | 仅产品模糊 | |
| 9 | 通用 + stage + scene | product_id="" 的模板 |
| 10 | 通用 + stage | |
| 11 | 通用 + scene | |
| 12 | 通用全空 | 意图级兜底，三要素全不限 |

> **模糊匹配**：模板 `product_id` 支持逗号/换行分隔多关键词，大小写不敏感，双向包含。比如模板写 `"套餐,升档"`，请求产品 ID 里只要含"套餐"或"升档"就命中。
>
> **通用模板**（product_id=""）：任何产品精确/模糊都没配上时自动走这里，按 stage/scene 继续匹配。
>
> **请求 product_id 本身就是空的**：跳过阶段 1/2，直接从阶段 3 开始。

### 5.4 漏传 stage/scene 也有兜底

严格匹配要求 "请求没传 stage → 模板 stage 也必须为空"。如果调用方漏传 stage/scene 导致 12 档全落空，系统会自动再宽松匹配一次：把请求中为空的维度视为 "不限"，只约束传了值的维度。日志会出现：

```
[ScriptStep] 宽松兜底命中 ...（请求未给定的维度按不限处理）
```

### 5.5 全没命中也不会报错

如果连 product_id="" 的通用模板都不存在，系统不会停止，而是逐层降级：

```
① 模板匹配命中 → 用模板正文 + 结构化 Prompt
② 全未命中 → 用 biz_config.prompts.package_recommendation 旧格式 Prompt
③ 旧格式也没有 → 用代码内置的兜底 Prompt 模板（含完整上下文变量）
④ 兜底也失败 → 纯字符串拼接保底
```

LLM 始终会被调用，只是没有省份定制的模板正文引导，话术质量不可控。

### 5.6 Expand 枚举模式（特殊场景）

`batch_contexts[].expand=true` 且 `scence` 为空时触发，行为不同：

- 只按**产品精确 + stage** 找所有 scene 非空的模板，每个 scene 各生成一条话术
- 不支持模糊匹配、不支持宽松兜底、排除 offline
- 产品专属没找到时降级到通用产品（product_id=""）的模板

### 5.7 入参 → 匹配结果速查

```text
传入 batch_contexts[{ stage: "营销推荐", scence: "营销推荐-融合权益" }]

① 有 recommended_packages（如 offerId="GD5G59"）→ 对所有产品逐个生成

② 模板匹配：拿 offerId="GD5G59" 去找
   ├── 有 product_id="GD5G59" + stage="营销推荐" + scene="营销推荐-融合权益" → 命中
   ├── 没有 → 尝试 offerName 等产品名称候选
   ├── 还没有 → 找 product_id="" + stage="营销推荐" + scene="营销推荐-融合权益" → 命中通用模板
   └── 连通用模板都没有 → 降级到旧 Prompt / 内置兜底，仍然会生成话术

③ 每个产品 × 命中的模板 = 一条话术
```
